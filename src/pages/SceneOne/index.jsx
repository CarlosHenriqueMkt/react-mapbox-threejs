import React from "react";
import GroundFloor from "../../components/models/GroundFloor";

export default function Scene1({ setOpenDrawerId }) {
	return <GroundFloor setOpenDrawerId={setOpenDrawerId} />;
}
